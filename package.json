{
  "name": "prompt-monitoring-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension to monitor ChatGPT prompts and anonymize emails.",
  "type": "module",
  "scripts": {
    "build:content": "vite build -c vite.content.config.ts",
    "build:inpage": "vite build -c vite.inpage.config.ts",
    "build:background": "vite build -c vite.background.config.ts",
    "build": "rm -rf build && npm run build:content && npm run build:inpage && npm run build:background && cp manifest.json build/",
    "zip": "npm run build && cd build && zip -r ../build.zip ."
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.269",
    "@types/node": "^24.10.1",
    "@types/react": "^18.2.66",
    "@types/react-dom": "^18.2.22",
    "@vitejs/plugin-react": "^4.2.1",
    "autoprefixer": "^10.4.18",
    "cssnano": "^7.1.2",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.13",
    "typescript": "^5.5.4",
    "vite": "^5.4.9"
  }
}
